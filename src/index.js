import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/App';

// рендер-отрисовка (что рисуем? куда рисовать)

ReactDOM.render(<App />, document.getElementById('root'));